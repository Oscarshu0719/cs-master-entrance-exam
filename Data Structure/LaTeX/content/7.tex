\item \begin{theorem}{(195)} Quick sorting：
    \begin{algorithm}[H]
        \begin{algorithmic}[1]
            \Function{QuickSort}{Array $A$, index $p$, $r$} \Comment{Sorting from $A[p]$ to $A[r]$}
                \If {$p < r$}
                    \State $q$ := \Call{Partition}{$A$, $p$, $r$}
                    \State \Call{QuickSort}{$A$, $p$, $q - 1$}
                    \State \Call{QuickSort}{$A$, $q + 1$, $r$}
                \EndIf
            \EndFunction
        \end{algorithmic}
    \end{algorithm}
    \begin{algorithm}[H]
        \begin{algorithmic}[1]
            \Function{Partition}{Array $A$, index $p$, $r$}
                \State $x$ := $A[r]$ \Comment{Pivot.}
                \State $i$ := $p - 1$
                \For {$j$ := $p$ to $r - 1$}
                    \If {$A[j] \le x$}
                        \State $i$ := $i + 1$
                        \State \Call{Swap}{$A[i]$, $A[j]$}
                    \EndIf
                \EndFor
                \State \Call{Swap}{$A[r]$, $A[i + 1]$}
                \State \Return $i + 1$
            \EndFunction
        \end{algorithmic}
    \end{algorithm}
\end{theorem}
